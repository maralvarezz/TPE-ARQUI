#include "./include/snakes.h"


void startGame(char jug){
    if(jug == '1'){
       // playGame1();
    }
    else{
        //playGame2();
    }
}

/*

typedef struct player{
    int posX;
    int posY;
    int dirX;
    int dirY;
    int points;
}player;

typedef struct player * TPlayer;

    void playGame1(){
        char l = 1;
        TPlayer p1;
        while(l){
            movePlayer(p1);
        }
    }

    void playGame2(){
        char l1 = 1, l2 = 1;
        TPlayer p1, p2;
        while(l){
            movePlayer(p1);
            movePlayer(p2);
        }
    }

    //devulve 0 si no hay colision
    int checkCollision(TPlayer p){)
    }

    void movePlayer(TPlayer p){
        if(checkCollision(p)){
            l = 0;
        }    
    }
*/


